import { PageShell } from "~/app/components/ui/PageShell";

export function BoardPage() {
  return (
    <PageShell
      title="Announcements Board"
      description="Public announcements will appear here. Wire up GET /api/v1/announcements when ready."
    />
  );
}
